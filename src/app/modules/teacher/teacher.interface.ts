export type TTeacher = {
  teacherName: string;
  email: string;
  salary: number;
  profileImg?: string;
  phone?: string;
  // subject: string;
  // qualifications: string[];
  // joiningDate: string;
  // createdAt?: Date;
  // updatedAt?: Date;
};
